# name: CI/CD Pipeline Auth-Service
# on:
#     push:
#         branches: ["main"]
# jobs:
#     build-test-deploy:
#         runs-on: ubuntu-latest

#         steps:
#             - name: Checkout Code
#               uses: actions/checkout@v3

#             - name: Setup Node.js
#               uses: actions/setup-node@v3
#               with:
#                 node-version: '18'
            
#             - name: Cache npm
#               uses: actions/cache@v3
#               with:
#                 path: ~/.npm
#                 key: ${{runner.os}}-node-${{hashFiles('package-lock.json')}}


#             - name: Install Dependencies
#               run: npm install

#             - name: Run Tests
#               run: npm test
#               env:
#                 NODE_ENV: test          
            
#             - name: Build Docker Image
#               run: docker build --pull -t ${{secrets.DOCKER_USERNAME}}/auth-service:latest .

#             - name: Login to DockerHub
#               run: echo "${{secrets.DOCKER_PASSWORD}}" | docker login -u "${{secrets.DOCKER_USERNAME}}" --password-stdin
            
#             - name: Push Image
#               run: docker push ${{secrets.DOCKER_USERNAME}}/auth-service:latest

#             - name: Deploy to Azure VM
#               uses: appleboy/ssh-action@v0.1.6
#               with:
#                 host: ${{secrets.SERVER_HOST}}
#                 username: ${{secrets.SERVER_USER}}
#                 key: ${{secrets.SERVER_SSH_KEY}}
#                 script: |
#                  docker pull ${{secrets.DOCKER_USERNAME}}/auth-service:latest
#                  docker stop auth-service || true
#                  docker rm auth-service || true
#                  docker run -d --env-file /home/azureuser/auth.env -p 5000:5000 --restart unless-stopped --name auth-service ${{secrets.DOCKER_USERNAME}}/auth-service:latest


name: CI/CD - Auth Service

on:
  push:
    branches: ["main"]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # =========================
      # 1. Checkout
      # =========================
      - name: Checkout Code
        uses: actions/checkout@v3

      # =========================
      # 2. Setup Node
      # =========================
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      # =========================
      # 3. Cache npm
      # =========================
      - name: Cache npm
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}

      # =========================
      # 4. Install + Test
      # =========================
      - name: Install Dependencies
        run: npm install

      - name: Run Tests
        run: npm test

      # =========================
      # 5. Build Docker Image
      # =========================
      - name: Build Docker Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/auth-service:latest .

      # =========================
      # 6. Login DockerHub
      # =========================
      - name: Login to DockerHub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      # =========================
      # 7. Push Image
      # =========================
      - name: Push Docker Image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/auth-service:latest

      # =========================
      # 8. Trigger Deploy Repo 
      # =========================
      - name: Trigger Deployment
        run: |
          curl -X POST https://api.github.com/repos/gagan-upadhyay/expense-tracker-deploy/dispatches \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer ${{ secrets.DEPLOY_REPO_TOKEN }}" \
          -d '{"event_type":"deploy"}'